{% extends 'layout.html' %}
{% block content %}

{% if songs %}
    <ul>
        {% for song in songs %}
            <li>
                <p>{{ song }}</p>
                <audio class="audio" controls>
                    <source src="{{ url_for('static', filename='uploads/'+song) }}" type="audio/mpeg">
                </audio>
            </li>
        {% endfor %}
    </ul>
    <p>Want to upload more music?</p>
    <a href="{{ url_for('upload_page') }}" class="btn btn-danger">Upload now</a>
{% else %}
    <p>Why not upload some music?</p>
    <a href="{{ url_for('upload_page') }}" class="btn btn-danger">Upload now</a>
{% endif %}

{% endblock %}
